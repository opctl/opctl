{
  "type": "object",
  "properties": {
    "inputs": {
      "$ref": "inputs.json"
    },
    "outputs": {
      "$ref": "outputs.json"
    },
    "pullCreds": {
      "$ref": "../pullCreds.json"
    },
    "ref": {
      "description": "Reference to an op",
      "type": "string",
      "format": "uri-reference"
    },
    "src": {
      "description": "Source code of the op to run. MUST be a valid op directory.",
      "type": "string"
    }
  },
  "oneOf": [
    {
      "required": [
        "ref"
      ]
    },
    {
      "required": [
        "src"
      ]
    }
  ],
  "additionalProperties": false
}