version: "2"
services:
  build-docker-image:
    extends:
      file: ../.common/docker-compose.yml
      service: docker
    build:
      context: ../../../
      dockerfile: .dev-op-spec/ops/build-docker-image/Dockerfile
    image: opctl/engine
    entrypoint: echo # noop
