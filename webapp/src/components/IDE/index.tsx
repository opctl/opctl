import React from 'react'
import {css} from 'emotion'

export default () => {

    return (
        <div
            className={css({
                display: 'flex'
            })}
        >
            
        </div>
    )
}