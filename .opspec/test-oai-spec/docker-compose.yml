version: "2"
services:
  test-oai-spec:
    build: .
    volumes:
      - ../../:/working_dir
    working_dir: /working_dir
    entrypoint: |
      sh -c "
        swagger validate runtime-oai_spec.yml
        swagger validate distribution-oai_spec.yml
      "

