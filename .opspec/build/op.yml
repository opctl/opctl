description: runs a build
name: build
inputs:
  srcDir:
    dir:
      default: .
      description: directory containing the projects source code
run:
  op:
    pkg: { ref: test }
    inputs: { srcDir }
