version: "2"
services:
  generate-fakes:
    extends:
      service: go
      file: ../.common/docker-compose.yml
    entrypoint: |
      sh -c "
      go get github.com/maxbrunsfeld/counterfeiter && \
      go generate
      "
