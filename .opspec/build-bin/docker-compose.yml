version: "2"
services:
  build-bin:
    extends:
      service: go
      file: ../.common/docker-compose.yml
    entrypoint: govendor build
    environment:
      - CGO_ENABLED=0
    network_mode: none
