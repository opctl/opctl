version: "2"
services:
  test:
    extends:
      service: go
      file: ../.common/docker-compose.yml
    entrypoint: |
      sh -c "
        go get -t ./... && \
        go get github.com/onsi/ginkgo/ginkgo && \
        ginkgo -r -cover -race
      "
    environment:
      - CGO_ENABLED=1
