version: "2"
services:
  build-docker-image:
    extends:
      file: ../.common/docker-compose.yml
      service: docker
    build:
      context: ../../
      dockerfile: .opspec/build-docker-image/Dockerfile
    image: opctl/engine:$IMAGE_TAG
    entrypoint: echo # noop
