the dir contains opspec packages used to unit test package listing.

The package naming convention used is as follows:

(valid|invalid)(count)

examples:

| name    |
|:--------|
| valid   |
| valid1  |
| invalid |
