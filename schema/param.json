{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "id": "https://spec.opspec.io/0.1.3/schema/param.json",
  "title": "param",
  "description": "Parameter of an op",
  "oneOf": [
    {
      "$ref": "#/definitions/dirParam"
    },
    {
      "$ref": "#/definitions/fileParam"
    },
    {
      "$ref": "#/definitions/netSocketParam"
    },
    {
      "$ref": "#/definitions/stringParam"
    }
  ],
  "definitions": {
    "dirParam": {
      "title": "dirParam",
      "description": "Directory parameter of an op",
      "type": "object",
      "properties": {
        "description": {
          "description": "Description of the parameter",
          "type": "string"
        },
        "isSecret": {
          "description": "If the directory is secret",
          "type": "boolean"
        },
        "name": {
          "description": "Name of the parameter",
          "type": "string"
        }
      },
      "required": [
        "name"
      ],
      "additionalProperties": false
    },
    "fileParam": {
      "title": "fileParam",
      "description": "File parameter of an op",
      "type": "object",
      "properties": {
        "description": {
          "description": "Description of the parameter",
          "type": "string"
        },
        "isSecret": {
          "description": "If the file is secret",
          "type": "boolean"
        },
        "name": {
          "description": "Name of the parameter",
          "type": "string"
        }
      },
      "required": [
        "name"
      ],
      "additionalProperties": false
    },
    "netSocketParam": {
      "title": "netSocketParam",
      "description": "Network socket parameter of an op",
      "type": "object",
      "properties": {
        "description": {
          "description": "Description of the parameter",
          "type": "string"
        },
        "name": {
          "description": "Name of the parameter",
          "type": "string"
        }
      },
      "required": [
        "name"
      ],
      "additionalProperties": false
    },
    "stringParam": {
      "title": "stringParam",
      "description": "String parameter of an op",
      "type": "object",
      "properties": {
        "default": {
          "description": "Default value of the parameter",
          "type": "string"
        },
        "description": {
          "description": "Description of the parameter",
          "type": "string"
        },
        "isSecret": {
          "description": "If the string is secret",
          "type": "boolean"
        },
        "name": {
          "description": "Name of the parameter",
          "type": "string"
        },
        "pattern": {
          "description": "Pattern the value must match, as defined by json-schema's 'pattern' validation keyword",
          "type": "string"
        },
        "minLength": {
          "description": "Minimum length the value must be, as defined by json-schema's 'minLength' validation keyword",
          "type": "integer"
        },
        "maxLength": {
          "description": "Maximum length the value must be, as defined by json-schema's 'maxLength' validation keyword",
          "type": "integer"
        }
      },
      "required": [
        "name"
      ],
      "additionalProperties": false
    }
  }
}
