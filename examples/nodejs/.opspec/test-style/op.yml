name: test-style
description: Runs style tests
inputs:
  appDir:
    dir:
      default: .
      description: directory containing the app
run:
  container:
    cmd: [ node_modules/.bin/eslint, lib ]
    dirs:
      /opt/app: { bind: appDir }
    image: node:7.4
    workDir: /opt/app
