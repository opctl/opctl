name: test-acceptance
description: Runs acceptance tests
inputs:
  - dir: { name: APP_DIR, description: Directory containing the app }
  - socket: { name: API_SOCKET, description: Socket for the API under test }
run:
  container:
    cmd: [ node_modules/.bin/mocha, --recursive, --reporter=spec, tests/unit ]
    dirs:
      /opt/app: { bind: APP_DIR }
    image: node:7.3
    sockets:
      api:80: { bind: API_SOCKET }
    workDir: /opt/app
