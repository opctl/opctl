- name: doesn't match format
  call:
    expect: failure
    scope:
      string:
        string: a
- name: doesn't match format
  interpret:
    expect: failure
    scope:
      string:
        string: a
- validate:
    expect: success
