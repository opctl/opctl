- name: doesn't match pattern
  call:
    expect: failure
    scope:
      string:
        string: a
- name: doesn't match pattern
  interpret:
    expect: failure
    scope:
      string:
        string: a
- validate:
    expect: success
