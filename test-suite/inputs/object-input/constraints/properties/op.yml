name: inputs/object-input/constraints/properties
inputs:
  object:
    object:
      constraints:
        properties:
          a: { enum: [a]}
          b: { enum: [b]}
run:
  container:
    image: { ref: alpine }
    cmd: [echo]
