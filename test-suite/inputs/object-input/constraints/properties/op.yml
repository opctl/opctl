name: inputs/object-input/constraints/properties
inputs:
  object:
    object:
      constraints:
        properties:
          a: { enum: [a]}
          b: { enum: [b]}
run:
  container:
    image: { ref: ghcr.io/linuxcontainers/alpine }
    cmd: [echo]
