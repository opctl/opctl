name: inputs/object-input/constraints/patternproperties
inputs:
  object:
    object:
      constraints:
        patternProperties:
          a: { enum: [a]}
          b: { enum: [b]}
run:
  container:
    image: { ref: ghcr.io/linuxcontainers/alpine }
    cmd: [echo]
