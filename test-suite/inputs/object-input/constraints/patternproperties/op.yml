name: inputs/object-input/constraints/patternproperties
inputs:
  object:
    object:
      constraints:
        patternProperties:
          a: { enum: [a]}
          b: { enum: [b]}
run:
  container:
    image: { ref: alpine }
    cmd: [echo]
