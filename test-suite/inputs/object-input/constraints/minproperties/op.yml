name: inputs/object-input/constraints/minproperties
inputs:
  object:
    object:
      constraints:
        minProperties: 2
run:
  container:
    image: { ref: ghcr.io/linuxcontainers/alpine }
    cmd: [echo]
