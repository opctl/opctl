name: inputs/object-input/constraints/additionalproperties
inputs:
  object:
    object:
      constraints:
        additionalProperties: { minLength: 2 }
run:
  container:
    image: { ref: ghcr.io/linuxcontainers/alpine }
    cmd: [echo]
