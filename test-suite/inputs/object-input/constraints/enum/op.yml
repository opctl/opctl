name: inputs/object-input/constraints/enum
inputs:
  object:
    object:
      constraints:
        enum:
          - { a: a }
run:
  container:
    image: { ref: ghcr.io/linuxcontainers/alpine }
    cmd: [echo]
