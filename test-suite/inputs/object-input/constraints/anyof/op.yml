name: inputs/object-input/constraints/anyof
inputs:
  object:
    object:
      constraints:
        anyOf:
          - required: [a]
          - minProperties: 2
run:
  container:
    image: { ref: ghcr.io/linuxcontainers/alpine }
    cmd: [echo]
