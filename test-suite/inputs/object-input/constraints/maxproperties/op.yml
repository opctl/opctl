name: inputs/object-input/constraints/maxproperties
inputs:
  object:
    object:
      constraints:
        maxProperties: 2
run:
  container:
    image: { ref: ghcr.io/linuxcontainers/alpine }
    cmd: [echo]
