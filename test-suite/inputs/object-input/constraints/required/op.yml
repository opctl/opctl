name: inputs/object-input/constraints/required
inputs:
  object:
    object:
      constraints:
        required: [ a, b ]
run:
  container:
    image: { ref: ghcr.io/linuxcontainers/alpine }
    cmd: [echo]
