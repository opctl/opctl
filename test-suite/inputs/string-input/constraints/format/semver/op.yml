name: inputs/string/constraints/format/semver
inputs:
  string:
    string:
      constraints:
        format: semver
run:
  container:
    image: { ref: ghcr.io/linuxcontainers/alpine }
    cmd: [echo]
