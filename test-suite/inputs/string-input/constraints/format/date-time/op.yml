name: inputs/string/constraints/format/date-time
inputs:
  string:
    string:
      constraints:
        format: date-time
run:
  container:
    image: { ref: ghcr.io/linuxcontainers/alpine }
    cmd: [echo]
