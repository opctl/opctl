name: inputs/string/constraints/format/docker-image-ref
inputs:
  string:
    string:
      constraints:
        format: docker-image-ref
run:
  container:
    image: { ref: ghcr.io/linuxcontainers/alpine }
    cmd: [echo]
