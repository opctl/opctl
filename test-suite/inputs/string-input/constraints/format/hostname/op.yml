name: inputs/string/constraints/format/hostname
inputs:
  string:
    string:
      constraints:
        format: hostname
run:
  container:
    image: { ref: ghcr.io/linuxcontainers/alpine }
    cmd: [echo]
