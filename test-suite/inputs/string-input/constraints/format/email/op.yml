name: inputs/string/constraints/format/email
inputs:
  string:
    string:
      constraints:
        format: email
run:
  container:
    image: { ref: ghcr.io/linuxcontainers/alpine }
    cmd: [echo]
