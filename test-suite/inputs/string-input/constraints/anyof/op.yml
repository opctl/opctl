name: inputs/string/constraints/anyof
inputs:
  string:
    string:
      constraints:
        anyOf:
          - pattern: ^$
run:
  container:
    image: { ref: ghcr.io/linuxcontainers/alpine }
    cmd: [echo]
