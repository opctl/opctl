name: inputs/string/constraints/not
inputs:
  string:
    string:
      constraints:
        not: { pattern: ^.*$ }
run:
  container:
    image: { ref: ghcr.io/linuxcontainers/alpine }
    cmd: [echo]
