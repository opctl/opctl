- name: doesn't meet any oneOf constraints
  call:
    expect: failure
    scope:
      string:
        string:
- name: doesn't meet any oneOf constraints
  interpret:
    expect: failure
    scope:
      string:
        string:
- validate:
    expect: success
