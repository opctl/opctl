name: inputs/string/constraints/oneof
inputs:
  string:
    string:
      constraints:
        oneOf:
          - pattern: ^$
run:
  container:
    image: { ref: ghcr.io/linuxcontainers/alpine }
    cmd: [echo]
