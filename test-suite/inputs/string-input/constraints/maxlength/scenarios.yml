- name: longer than maxLength
  call:
    expect: failure
    scope:
      string:
        string: aaaa
- name: longer than maxLength
  interpret:
    expect: failure
    scope:
      string:
        string: aaaa
- validate:
    expect: success
