name: inputs/string/constraints/allof
inputs:
  string:
    string:
      constraints:
        allOf:
          - pattern: ^$
run:
  container:
    image: { ref: ghcr.io/linuxcontainers/alpine }
    cmd: [echo]
