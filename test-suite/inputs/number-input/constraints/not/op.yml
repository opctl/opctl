name: inputs/number/constraints/not
inputs:
  number:
    number:
      constraints:
        not:
          enum: [2]
run:
  container:
    image: { ref: ghcr.io/linuxcontainers/alpine }
    cmd: [echo]
