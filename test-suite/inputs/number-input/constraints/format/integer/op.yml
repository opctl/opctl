name: inputs/number/constraints/format/integer
inputs:
  number:
    number:
      constraints:
        format: integer
run:
  container:
    image: { ref: ghcr.io/linuxcontainers/alpine }
    cmd: [echo]
