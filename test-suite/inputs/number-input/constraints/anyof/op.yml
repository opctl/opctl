name: inputs/number/constraints/anyof
inputs:
  number:
    number:
      constraints:
        anyOf:
          - enum: [1]
          - maximum: 2
run:
  container:
    image: { ref: alpine }
    cmd: [echo]
