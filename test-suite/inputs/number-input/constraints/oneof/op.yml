name: inputs/number/constraints/oneof
inputs:
  number:
    number:
      constraints:
        oneOf:
          - maximum: 2
run:
  container:
    image: { ref: ghcr.io/linuxcontainers/alpine }
    cmd: [echo]
